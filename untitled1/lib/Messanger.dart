import 'package:flutter/material.dart';

class MessangerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Massanger Page'),
      ),
      body: Center(
        child: Text('This is the Messanger Page'),
      ),
    );
  }
}
